Simple box example:
- when clients disconnects, their player entity is not despawned
- when I stop the server, the player entities are not despawned
- same on the client, the player entity is still present when disconnecting (even though they should be despawned via Control)
- when multiple clients are present, input commands don't work

- bevy_inspector_egui shows all Observers. Can we upgrade it to the latest version? how can we hide observers?
